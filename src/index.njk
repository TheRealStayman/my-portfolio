---
title: "Home"
layout: "layout.njk"
---

<div class="banner">
    <div class="banner-content">
        <div class="banner-text">
            <h1>{{ site.name }}</h1>
            <p class="subtitle">{{ site.subtitle }}</p>
            <p>
                Welcome to my personal portfolio. Here you'll find a showcase of my work in software development, firmware engineering, and game design, along with my professional experiences and leadership roles. Feel free to explore and get in touch!
            </p>
        </div>
        <div class="banner-image">
            <img src="/img/your-banner-image.png"
                 alt="A decorative image of Thomas Stay">
        </div>
    </div>
</div>
<div class="content-tile">
    <div class="content-background">
        <div class="education-skills-tile">
            <div class="tile-section">
                <div class="text-block">
                    <h2>Education</h2>
                    <p>
                        <strong>University of Excellence</strong>
                        <br>
                        Master of Science in Computer Engineering
                        <br>
                        Graduated: May 20XX
                    </p>
                    <p>
                        <strong>Tech State University</strong>
                        <br>
                        Bachelor of Science in Software Development
                        <br>
                        Graduated: May 20XX
                    </p>
                </div>
                <div class="list-block">
                    <h3>Related Coursework</h3>
                    <ul>
                        <li>Advanced Algorithms</li>
                        <li>Embedded Systems Design</li>
                        <li>Game AI Programming</li>
                        <li>Compiler Construction</li>
                        <li>Network Security</li>
                    </ul>
                </div>
            </div>
            <div class="tile-section">
                <div class="text-block">
                    <h2>Skills</h2>
                    <p>
                        My expertise spans across various domains, from low-level firmware to high-level application and game development.
                    </p>
                </div>
                <div class="list-block">
                    <h3>Programming</h3>
                    <ul>
                        <li>C/C++</li>
                        <li>Python</li>
                        <li>JavaScript (Node.js, React)</li>
                        <li>C# (Unity)</li>
                        <li>Assembly</li>
                        <li>Java</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
{# <hr class="divider"> #}
<div class="carousel-section-wrapper">
    <div class="section-title-wrapper">
        <h2 class="section-title">Work Experience</h2>
    </div>
    <div class="carousel" id="work-experience-carousel">
        <div class="first-card"></div>
        {% for job in work_experience %}
        {% set imageSources = "" %}
        {% if job.Images %}
            {% for image in job.Images %}
                {% if loop.first %}
                    {% set imageSources = image.src %}
                {% else %}
                    {% set imageSources = imageSources + "," + image.src %}
                {% endif %}
            {% endfor %}
        {% endif %}
        <div class="card" 
             data-category="work"
             data-full-description="{{ job['Description Long'] }}"
             data-additional-images="{{ imageSources }}">
            {% if job['Header Image'] %}
            <img src="{{ job['Header Image'].src }}"
                 alt="{{ job['Header Image'].alt }}"
                 class="card-image">
            {% else %}
            <img src="/img/placeholder-image.png"
                 alt="{{ job.Title }}"
                 class="card-image">
            {% endif %}
            <div class="card-content">
                <h3>{{ job.Title }}</h3>
                <p class="card-subtitle">{{ job.Subtitle }}</p>
                <p>{{ job['Description Brief'] }}</p>
                <div class="card-footer">
                    <span class="location">{{ job.Location }}</span>
                    <span class="date">{{ job.Date }}</span>
                </div>
            </div>
        </div>
        {% endfor %}
        <div class="last-card"></div>
    </div>
</div>
{# <hr class="divider"> #}
<div class="carousel-section-wrapper">
    <div class="section-title-wrapper">
        <h2 class="section-title">Projects</h2>
    </div>
    <div class="carousel" id="projects-carousel">
        <div class="first-card"></div>
        {% for project in projects %}
        <div class="card">
            {% if project['Header Image'] %}
            <img src="{{ project['Header Image'].src }}"
                 alt="{{ project['Header Image'].alt }}"
                 class="card-image">
            {% else %}
            <img src="/img/placeholder-image.png"
                 alt="{{ project.Title }}"
                 class="card-image">
            {% endif %}
            <div class="card-content">
                <h3>{{ project.Title }}</h3>
                <p>{{ project['Description Brief'] }}</p>
                <div class="card-footer">
                    <span class="date">{{ project.Date }}</span>
                    {% if project.Buttons and project.Buttons[0].text == "GitHub" %}
                    <a href="{{ project.Buttons[0].url }}"
                        class="github-link"
                        target="_blank"
                        rel="noopener noreferrer">View on GitHub</a>
                    {% endif %}
                </div>
            </div>
            <a href="{{ project['Card Link'] }}" class="stretched-card-link" aria-label="View project: {{ project.Title }}"></a>
        </div>
        {% endfor %}
        <div class="last-card"></div>
    </div>
</div>
{# <hr class="divider"> #}
<div class="carousel-section-wrapper">
    <div class="section-title-wrapper">
        <h2 class="section-title">Leadership and Service</h2>
    </div>
    <div class="carousel" id="leadership-carousel">
        <div class="first-card"></div>
        {% for item in leadership_and_service %}
        {% set imageSources = "" %}
        {% if item.Images %}
            {% for image in item.Images %}
                {% if loop.first %}
                    {% set imageSources = image.src %}
                {% else %}
                    {% set imageSources = imageSources + "," + image.src %}
                {% endif %}
            {% endfor %}
        {% endif %}
        <div class="card"
             data-category="leadership"
             data-full-description="{{ item['Description Long'] }}"
             data-additional-images="{{ imageSources }}">
            {% if item['Header Image'] %}
            <img src="{{ item['Header Image'].src }}"
                 alt="{{ item['Header Image'].alt }}"
                 class="card-image">
            {% else %}
            <img src="/img/placeholder-image.png"
                 alt="{{ item.Title }}"
                 class="card-image">
            {% endif %}
            <div class="card-content">
                <h3>{{ item.Title }}</h3>
                <p class="card-subtitle">{{ item.Subtitle }}</p>
                <p>{{ item['Description Brief'] }}</p>
                <div class="card-footer">
                    <span class="location">{{ item.Location }}</span>
                    <span class="date">{{ item.Date }}</span>
                </div>
            </div>
        </div>
        {% endfor %}
        <div class="last-card"></div>
    </div>
</div>
{# <hr class="divider"> #}
<div class="carousel-section-wrapper">
    <div class="section-title-wrapper">
        <h2 class="section-title">Other Accomplishments</h2>
    </div>
    <div class="carousel" id="accomplishments-carousel">
        <div class="first-card"></div>
        {% for accomplishment in accomplishments %}
        <div class="card no-pop">
            <img src="/img/accomplishment-placeholder.png"
                 alt="{{ accomplishment.Title }}"
                 class="card-image">
            <div class="card-content">
                <h3>{{ accomplishment.Title }}</h3>
                <p>{{ accomplishment['Description Brief'] }}</p>
                <div class="card-footer">
                    {% if accomplishment.Date %}
                    <span class="date">{{ accomplishment.Date }}</span>
                    {% endif %}
                    {% if accomplishment['Card Link'] %}
                    <a href="{{ accomplishment['Card Link'] }}" 
                       class="github-link" 
                       target="_blank" 
                       rel="noopener noreferrer">Learn More</a>
                    {% endif %}
                </div>
            </div>
        </div>
        {% endfor %}
        <div class="last-card"></div>
    </div>
</div>
<div class="content-tile">
    <div class="content-background">
        <div class="contact-tile">
            <h2>Contact Me</h2>
            <p>
                I'm always open to discussing new opportunities, collaborations, or just chatting about technology. Feel free to reach out!
            </p>
            <div class="contact-links">
                <p>
                    Email: <a href="{{ site.email }}">{{ site.email | replace('mailto:', '') }}</a>
                </p>
                <p>
                    LinkedIn: <a href="{{ site.linkedin }}" target="_blank" rel="noopener noreferrer">{{ site.linkedin }}</a>
                </p>
                <p>
                    GitHub: <a href="{{ site.github }}" target="_blank" rel="noopener noreferrer">{{ site.github }}</a>
                </p>
            </div>
        </div>
    </div>
</div>
