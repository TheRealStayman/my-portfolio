<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{{ title or site.name }}</title>
        <link rel="stylesheet" href="/css/style.css">
    </head>
    <body class="page-with-sidebar">
        <aside class="page-sidebar">
            <nav class="sidebar-nav">
                <ul>
                    {%- for item in navigation -%}
                        <li>
                            <a href="{{ item.url | url }}"
                               class="sidebar-link{% if item.url == page.url %} active-link{% endif %}">
                                <span class="link-text">{{ item.text }}</span>
                                {%- if item.url == page.url -%}
                                    <span class="active-indicator"></span>
                                {%- endif -%}
                            </a>
                        </li>
                    {%- endfor -%}
                </ul>
            </nav>
            <div class="sidebar-social-links">
                <a href="{{ site.linkedin }}"
                   title="LinkedIn"
                   target="_blank"
                   rel="noopener noreferrer">LI</a>
                <a href="{{ site.github }}"
                   title="GitHub"
                   target="_blank"
                   rel="noopener noreferrer">GH</a>
                <a href="{{ site.email }}" title="Email">EM</a>
            </div>
        </aside>
        <div class="main-content-area">
            <header class="page-title-header">
                <h1>{{ title }}</h1>
            </header>
            <main>
                {{ content | safe }}
            </main>
        </div>
        {# Ripple effect div from homepage layout - consider if needed on these pages or a more global solution #}
        {# For now, assuming it's mainly for homepage. If needed here, uncomment and adjust JS if necessary #}
        {# <div id="click-ripple"></div> #}
        <script src="/js/main.js"></script> {# Assuming main.js is still relevant for other global scripts if any #}
    </body>
</html>
